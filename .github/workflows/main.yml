name: Build with Hugo

on: [push]

jobs:
  hugo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Init
        run: |
          make init build

      - name: Run Monako
        run: ./monako
